<h1 class="align-center">Welcome to <%= @user.username %>'s page </h1>
<div class="row">
	<div class="col-md-4 col-md-offset-4 align-center">
		<%= gravatar_for(@user, size: 100) %>
	</div>
</div>

<h4 class="align-center"><%= @user.username %>'s articles</h4>

<%= will_paginate @user_articles, class: "align-center" %>

<%= render "articles/articles", obj: @user_articles %>

<%= will_paginate @user_articles, class: "align-center" %>